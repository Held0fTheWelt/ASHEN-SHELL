# Runtime aspect contracts

Bounded runtime contracts (Pi-scoped). Each file defines validation, ledger propagation, and gate
oracle boundaries. Owning narrative: [ai-stack SAD](../components/ai-stack/architecture.md) and
[world-engine SAD](../components/world-engine/architecture.md).

Contracts are migrated from `docs/technical/runtime/` with redirect stubs left at old paths.

**Single source index:** [normative contracts index](../../dev/contracts/normative-contracts-index.md) (section **Runtime aspect contracts (Pi-scoped)**) lists all 25 runtime contracts plus top-level `turn_execution_contract.md` and `session_authority_contract.md`.
