Implement {MVP_FILE_NAME} exactly as documented in /mvps.

Rules:
- Only perform work that belongs to this MVP.
- Keep the codebase runnable.
- Do not start later MVPs.
- Update the MVP file with:
  - changes made
  - files touched
  - decisions
  - status
- If a source file is too domain-specific, replace it instead of preserving it.
- Keep code and identifiers in English.
- Do not reintroduce movie or blog domain logic.

At the end:
1. verify the result against the acceptance criteria in the MVP file
2. list the files changed
3. note any blockers inside the MVP file
