# Usabilify state

This folder holds the stable, human-readable state for the suite.

- `USABILITY_STATE.md` is the current summary of the latest inventory/evaluation wave.
- Detailed machine outputs live in `../reports/`.
