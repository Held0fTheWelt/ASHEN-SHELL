# Observifyfy Next Steps

- highest_value_next_step: **contractify** — Add or repair a workflow for contractify.

## Ranked actions

- P90 `contractify` — Add or repair a workflow for contractify.
- P90 `docify` — Add or repair a workflow for docify.
- P90 `despaghettify` — Add or repair a workflow for despaghettify.
- P90 `templatify` — Add or repair a workflow for templatify.
- P90 `usabilify` — Add or repair a workflow for usabilify.
- P90 `securify` — Add or repair a workflow for securify.
- P90 `mvpify` — Add or repair a workflow for mvpify.
- P88 `mvpify` — Keep mvpify imports normalized and mirrored so temporary implementation folders can be removed safely.
- P85 `contractify` — Keep contractify's internal ADR management discoverable through observifyfy and align references to docs/ADR.
- P85 `documentify` — Keep documentify's internal docs management discoverable through observifyfy and align references to docs.
- P84 `metrify` — Keep metrify ledgers and reports fresh so AI spend and value stay visible.
- P70 `observifyfy` — Run observifyfy at least once and capture evidence.
