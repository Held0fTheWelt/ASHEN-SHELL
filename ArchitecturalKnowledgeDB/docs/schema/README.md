# Schema

This folder contains the SQLite schema reference for AKDB.

| File | Purpose |
| --- | --- |
| `DATA_MODEL_SQLITE_MULTI_PROJECT.sql` | Multi-project database model reference. |

Runtime migrations live in `architectural_knowledge_db/db/schema/`. Keep this schema reference synchronized when the persisted model changes in a way users or integrators must understand.
