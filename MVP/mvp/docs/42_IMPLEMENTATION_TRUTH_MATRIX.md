# 42 — Implementation Truth Matrix

## Repository-strong
- backend, frontend, administration-tool, world-engine, writers-room, AI stack, and repo tests already exist as real project surfaces
- tools/mcp_server exists as a real MCP registry/handler package
- repository service boundaries are real rather than hypothetical
- content/compiler and module flow already exist as concrete project concerns

## MVP-strong
- this package contains the canonical target definition
- this package contains implementation rules and repository ownership maps
- this package contains workstreams, validation commands, and deliverable rules
- this package contains a runnable reference scaffold with tests

## Executable-proof-strong
- the scaffold contains real code, not pseudocode
- the scaffold runs as an HTTP service and persistence-backed runtime slice
- the scaffold includes tests for the named constitutional seams

## Requires repository implementation work
- any behavior that is defined here but not yet present in the owning repository component
- any seam where repository code, repository tests, and repository docs still disagree
- any production-hardening concern that depends on service-specific integration rather than the scaffold proof slice

## Honesty rule
A claim is only implementation-true when the owning component code and the matching validation evidence both exist.
